[project]
name = "anshlamba-dbt-projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Borokoro" }
]
requires-python = ">=3.12,<3.13"
dependencies = [
    "dbt-core==1.10.13",
]

[project.scripts]
anshlamba-dbt-projects = "anshlamba_dbt_projects:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
