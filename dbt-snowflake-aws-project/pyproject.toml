[project]
name = "dbt-snowflake-aws-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Borokoro" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.5",
    "dbt-snowflake>=1.12.1",
]

[project.scripts]
dbt-snowflake-aws-project = "dbt_snowflake_aws_project:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
